# lit-review 使用指南

[English](USAGE.md) | **繁體中文**

> 給第一次使用的人：這份指南從安裝到讀懂報告，照順序看完約十分鐘。

## 這是什麼(30 秒版)

一個讓 LLM agent(Claude、Codex 或其他)變成嚴謹文獻助手的 skill。它做三件事：

1. **找文獻**：貼一段草稿，它萃取論點、搜學術資料庫、推薦真實存在的文獻，附摘要證據
2. **查核引用**：丟一份有參考文獻的文章，它逐筆驗證——文獻存在嗎？書目對嗎？真的支持你掛它的那句話嗎？
3. **文獻支撐寫作**：給一個主題，它先檢索再寫作，產出每個宣稱都有文獻可查的文章

核心賣點是**誠實**：查不到就說查不到、判不了就標 ❓、所有書目欄位來自 API 而非模型記憶。它的任務不是讓你的文章看起來有很多引用，是讓每個引用都經得起口試委員追問。

## 安裝

**Claude Code**(桌面版或 CLI):

```bash
git clone https://github.com/<repo>/lit-review-skill ~/.claude/skills/lit-review
```

裝完即用，不需重啟。腳本是純 Python 標準函式庫(3.8+)，不用 pip 裝任何東西。

**Codex / 其他 agent**:clone 到任意位置，在專案的 `AGENTS.md` 加一行：

> 文獻搜尋與引用查核請讀 `<clone路徑>/SKILL.md` 並照其流程執行。

## 五分鐘上手：三個例子

**例 1：幫段落找文獻**——直接把草稿丟給 agent:

> 這是我論文的一段：「大學生的手機使用可能影響睡眠品質與學業表現……」幫這段找文獻

你會拿到：每個論點 2–3 篇推薦(附摘要證據句、為何相關、建議引用位置)+ 一個可匯入 EndNote/Zotero 的 `new_refs.ris`。

**例 2：查核整章引用**——丟檔案：

> 幫我查核 第二章.docx 的參考文獻有沒有問題

你會拿到查核報告：總覽表(每筆的存在性/書目/支持度)+ 逐筆細節(哪個欄位錯、錯成什麼、摘要證據)+ 需人工處理清單。

**例 3：寫帶文獻的講解**:

> 寫一篇詳細講解 Transformer 模型的文章，要附參考文獻

它會先檢索、後寫作、寫完自查，產出的文章附「引用對照表」——每個掛引用的句子都列出摘要證據句，鋪陳性解說明確標示不掛引用。

## 指令速查表

不打指令也能用(agent 會從輸入推斷要做什麼)；要精確控制就用指令。Claude Code 打 `/lit-review <指令>`,Codex 直接打指令詞：

| 指令 | 功能 |
|---|---|
| `check <檔案>` | 查核引用(有列表自動加做找文獻) |
| `find <段落>` | 找文獻 |
| `write <主題>` | 文獻支撐寫作 |
| `verify <單筆引用>` | 單筆快查存在性+書目 |
| `map <主題>` | 領域地圖：奠基文獻/關鍵作者/近年走向 |
| `gap <X 與 Y>` | 研究缺口偵測 |
| `matrix <清單或主題>` | 文獻矩陣(方法/樣本/發現/限制對照表) |
| `notes <DOI>` | 單篇閱讀筆記卡 |
| `integrity <檔案>` | 文內引用 vs 列表三向核對(秒出、零成本) |
| `glossary <檔案>` | 中英術語一致性檢查 |
| `rehearse <檔案>` | 口試/審稿提問預演 |
| `watch <DOI 清單>` | 新文獻追蹤(Claude Code 可排程) |
| `annotate <檔案>` | 標記文章裡哪些句子需要引用 |
| `counter <論點>` | 主動找反面/零結果證據(誠實寫作用) |
| `strength <文獻+論點>` | 證據強度評級(統合分析 vs 橫斷面小樣本，一眼分清) |
| `claims <檔案>` | Claim–Evidence 總表：每個論點有幾篇支持/反對、證據多強 |
| `retract <DOI>` | 撤稿/更正查詢(`check` 已預設內含，零 LLM 成本) |
| `versions <arXiv或DOI>` | preprint↔正式版解析，有正式版就給 DOI 建議改引 |
| `export-xml <選定文獻>` | EndNote XML 匯出，查核結論(支持度/證據句/紅旗)一起進 Research Notes 欄 |

**修飾詞**(接在指令後):`deep` 深查(摘要判不動時抓開放取用全文)/ `quick` 最省模式 / `thorough` 逐句對抗驗證 / `bibtex` 要 BibTeX / `no-ris` 不要引用檔。例：`check deep 第二章.docx`。

## 金鑰設定(選配，但建議)

不設金鑰就能用，但 Semantic Scholar 免費共享池尖峰時段會塞車(自動改走 OpenAlex 備援，結果照樣可信，只是排序略噪)。在**專案目錄**建 `.env`:

```
S2_API_KEY=你的金鑰          # semanticscholar.org/product/api 免費申請，幾個工作天核發
CROSSREF_MAILTO=你的email    # 進 Crossref/OpenAlex 禮貌池，更穩
```

**`.env` 千萬不要 commit 進 git**(本 repo 的 .gitignore 已擋)。

## 成本控制(token 花費)

花錢的是 LLM 判讀，不是檢索(API 呼叫和 integrity 腳本零 token)。三檔驗證強度：

| 檔位 | 成本 | 適用 |
|---|---|---|
| `quick` | 1x | 日常草稿。跳過獨立審查，報告會標「未經獨立審查」 |
| 預設 | ~1.5–2x | 一般交付。主 agent + 一個獨立審查 agent |
| `thorough` | 5–10x | 口試前/投稿前的最終查核，逐句對抗驗證 |

原則：**驗證強度跟著錯誤代價走**。另外 agent 內部會用 brief/pick 兩段式讀取(先一行一筆瀏覽再精讀選中者)，同樣的任務比整包硬讀省一半以上。

## 讀懂報告

**支持度符號**:

- ✅ 支持：摘要(或全文)明確涵蓋該論點
- ⚠️ 部分支持：相關但你的措辭比證據強半格(最常見——例如文獻說「相關」你寫「導致」)。報告會告訴你「這篇撐得起哪半句」
- ❌ 疑似不支持：主題對不上，引了會被抓
- ❓ 無法判斷：無摘要可用(付費牆、出版社不給)。**這不是文獻有問題**，是自動查核的極限，附連結供人工
- 🚫 查無此文獻：三個資料庫都找不到。注意報告措辭是「三庫皆查無」而非「捏造」——書籍章節、很新的文章都可能查不到

**證據層級**(矩陣與深查報告會標):`[摘要]`、`[全文 p.X]`、`[書目]`、`[綜合判斷]`、`[❓]`——讓你知道每個判定有多可靠。

**品質紅旗**(`quality_warnings`):0 被引(有年齡門檻，新文獻不誤傷)、期刊不在 DOAJ/CORE 收錄等。紅旗不是否決，是「只能當佐證+報告必須明示」。

## 常見問題

**Q：跑很慢？**
無金鑰時 S2 限流，腳本在退避重試(正常)。申請免費金鑰可解；急用時結果會自動走 OpenAlex 備援。

**Q：中文文獻怎麼辦？**
學術 API 幾乎不收中文文獻。環境有 Google Scholar 工具時會用原文標題盡力查(標「信心中等」)；查無或沒工具就列「需人工查核」。它不會拿英譯標題去英文庫硬配——那會產生錯誤配對。

**Q：專書查不到？**
API 對專書(尤其 2010 年前)收錄很差，常只命中同名書評——書評存在反而間接證明書存在，報告會這樣標並提醒你核 ISBN。

**Q：它說我的引用「部分支持」，是要我刪掉嗎？**
多半不是。部分支持通常是措辭問題：把「顯著優於」改成「可改善」、把「導致」改成「與…有關」，引用就成立了。報告會給具體的措辭建議。

**Q：為什麼有的年份跟我查的差一年？**
線上先行(online-first)與正式卷期年差一年很常見，報告視為「無妨」不算錯；差兩年以上才當書目錯誤。arXiv 預印年 vs 會議正式年同理，報告會標明以哪個為準。

**Q：引用了 arXiv 版，報告叫我改？**
若該文已有正式發表版(期刊/會議)，學術慣例應引正式版，報告會附正式版的 DOI 讓你直接換。

## 誠實限制(用之前先知道)

1. 預設判讀基於**摘要**；「摘要沒寫」不等於「內文沒有」。關鍵引用用 `deep` 升級到開放取用全文；付費牆內的拿不到，就是 ❓
2. 判定是 LLM 做的，隔離紀律(證據隔離/角色隔離/注入隔離)降低了出錯率，但不是零——**拿去口試/投稿前，❓ 和 ⚠️ 的項目請人工過一遍**
3. 引用完整性檢查(integrity)只支援數字式引用([1]、[2]);APA 作者-年份格式需人工
4. 被引數是查核當日的快照，會變動
5. 這個工具讓錯誤引用更難發生，但**文獻的選擇與詮釋責任永遠在作者**——它是防護欄，不是代駕
